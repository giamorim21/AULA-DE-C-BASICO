#include <stdio.h>

/*faça um código que preencha uma tabela de tamanho 3 por 4
onde cada casas sera prenchida da seguinte forma [L]*4 + [C]
*/

void main(){
    int l,c,M[3][4], soma=0;
    int cont;

    for(l=0; l<3; ++l){
        for(c=0; c<4; ++c){
            cont = l*c;
            M[3][4] = cont;
        }}
     for(l=0; l<3; ++l){
        for(c=0; c<4; ++c){
            printf("&d",M[l][c]);
    }
     }
}
