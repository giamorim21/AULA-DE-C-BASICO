#include <stdio.h>

/*Crie um programa em que o usuário insira duas notas
e, em seguida, uma função verifique se cada nota está entre 0 e 11.
Se ambas as notas estiverem dentro desse intervalo,
o programa deve calcular a média e exibir o resultado ao usuário.
*/

char maiuscula(char x){
   return (char)(x - 32);
}
void main(){
printf("%c", maiuscula('b'));

}
